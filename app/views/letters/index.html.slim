H1 書簡
=link_to "書簡を作成", :new_letter
br
=link_to "戻る", :root

- @given_letters.each_with_index do |letter,i|
  div #{@sent_countries[i]}| #{letter.text}

- @sent_letters.each_with_index do |letter,i|
  div #{@given_countries[i]}| #{letter.text}
